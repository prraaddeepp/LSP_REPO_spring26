TaskManager collaborates with Task because the CRC cards show that TaskManager is responsible for storing tasks, adding new tasks, finding tasks by ID, and returning tasks by status, all of which require interacting with Task objects. Task does not collaborate with TaskManager because Task’s responsibilities are limited to storing task information, updating task status, and providing task details. Those responsibilities can be completed independently without managing or controlling TaskManager.